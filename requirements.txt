# ナンピノニクスの依存はこれだけ。判断ロジックは標準ライブラリで書く。
# テストも標準ライブラリの unittest のみで動く。
PyYAML>=6.0

# 記録の言語化は、既定では Claude Code CLI（claude -p）を呼ぶため追加の依存はない。
# agent.yaml の narrate.writer を api にする場合だけ、別途 `pip install anthropic` が要る。
