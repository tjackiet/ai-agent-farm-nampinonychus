# 記憶方式 — ナンピノニクス

> 保存先・保持期間の正は `agent.yaml` の `memory` セクションです。

## なぜ記憶を分けるのか

15分ごとに起動するエージェントは、毎回まっさらな状態から始まります。
過去をすべて読み直すことはできません（文脈量の制約）。

ただし、**階段の何段目にいるかは記憶しません。**
段数・クールダウン・当日の約定回数・平均取得単価は、すべて
`bitbank paper` の実測（`trade-history` と `pnl`）から毎回導出できます。
記憶は「何を考えたか」を残すためのものであり、状態を保持するためのものではありません。

そこで記憶を**現在・短期・中期・長期**の4つに分け、
毎回読むもの／必要なときだけ読むものを明確に分けます。

## 記憶の構造

| 層       | ファイル                        | 内容                   | 毎回読む      | 保持     |
| -------- | ------------------------------- | ---------------------- | ------------- | -------- |
| 現在     | `bitbank paper` の実測          | 今の状態そのもの       | ✅ 毎回導出   | 取引所側 |
| 短期     | `var/memory/decisions/{date}.jsonl` | 15分ごとの判断1行      | ✅ 直近16件   | 90日     |
| 中期     | `var/memory/daily/{date}.md`        | 1日の要約              | ✅ 直近7日    | 無期限   |
| 長期     | `var/memory/lessons.md`             | 建玉1回ぶんの学び      | ✅ 全件       | 無期限   |

毎回の読み込みは、この4つだけです。約定履歴の全量や過去の判断ログを
遡って読むことはしません。必要なら `bitbank paper trade-history` を叩きます。

### なぜ「直近16件」なのか

16件 × 15分 = 4時間。クールダウン（30分）の判定には十分ですが、
クールダウンの期限は直近の買い約定時刻から毎回計算するため、記憶に頼りません。
短期記憶は「直前に何を考えていたか」を思い出すためだけに使います。

## 現在：`bitbank paper` の実測

**状態の正は取引所側（ペーパー口座）にあります。** 判断に必要な状態は、
毎回の実行で以下から導出します。導出の対応表は
[`docs/IMPLEMENTATION_PLAN.md`](docs/IMPLEMENTATION_PLAN.md) Phase 6 にあります。

| 取得元                       | 導出するもの                                     |
| ---------------------------- | ------------------------------------------------ |
| `bitbank paper trade-history` | 階段の段数、直近の約定価格と時刻、当日の約定回数、建玉の開始時刻 |
| `bitbank paper pnl`           | 建玉数量、平均取得単価、実現損益、含み損益        |
| `bitbank paper active-orders` | 未約定の買い・売り指値（本数の判定もここで行う）  |
| `bitbank paper assets`        | 現金残高（`available` = `total` - `locked`）      |

こうする理由は、**壊れないためです。** 実行が途中で落ちても、二重に起動しても、
人間が手で注文を出しても、次の実行は取引所側の事実から状態を組み直します。
記憶したファイルを正にすると、そのすべてで実際の口座と食い違います。

## スナップショットの位置づけ

実行のたびに **`var/status.yaml`**（Git 管理外）を書き出します。これは
状態の正ではなく、人間と HTML ステータス画面が読むためのスナップショットです。

リポジトリ直下の `status.yaml` は**スキーマの見本**であり、実行では書き換えません。
運用の産物を追跡してしまうと、15分ごとの実行のたびに作業ツリーが汚れ、
git の操作が止まるためです。記録は `var/` にまとめます。

- 毎回の実行の最後に、導出した状態で上書きする
- 観測できなかった値は `null`。**それらしい値で埋めない**
- 実行が途中で失敗した場合は更新しない（古い状態のほうが、嘘の状態より安全）
- **判断の入力としてスナップショットを読まない。** 読むと、上の「壊れない」性質が失われる

導出した状態とスナップショットの内容が食い違っていても、判断は導出側を使います。
食い違い自体は異常の兆候なので、判断ログに残します。

## 短期：判断ログ

`var/memory/decisions/2026-08-05.jsonl` に、実行1回につき1行を追記します。

```json
{
  "run_id": "2026-08-05T09:15:00+09:00",
  "state": "LADDERING",
  "pair": "btc_jpy",
  "price": 15230000,
  "anchor": 15900000,
  "drop_from_anchor_pct": -4.21,
  "position": { "amount": 0.005498, "avg_cost": 14550000 },
  "action": "HOLD",
  "reason": "クールダウン中。次段の発注は 15:20 以降",
  "orders": [],
  "sources": [
    { "cmd": "bitbank ticker btc_jpy --format=json --machine", "at": "2026-08-05T09:15:02+09:00" },
    { "cmd": "bitbank candles btc_jpy --type=1day --format=json --machine", "at": "2026-08-05T09:15:03+09:00" }
  ]
}
```

`action` が `HOLD` でも必ず記録します。
**何もしなかったことも判断**であり、あとから見返す価値があります。

## 中期：日次サマリ

毎日 23:50 に、その日の判断ログを `var/memory/daily/2026-08-05.md` にまとめます。

```markdown
# 2026-08-05

- 状態: LADDERING（2段目）
- 価格: 高値 15,900,000 / 安値 15,050,000 / 終値 15,230,000
- 判断: BUY 1件 / HOLD 95件
- 約定: 2段目 14,113,500 × 0.007086（100,000 JPY）
- 平均取得単価: 14,550,000 → 14,300,000
- 含み損益: -68,000 JPY (-3.8%)
- 総資産: 962,000 JPY (-3.8%)
- 所感: アンカーが切り下がった。3段目の指値は 13,548,960 に置いた。
```

日次サマリは、実際に何が起きたかを人間が追える最小単位です。

### 書けるところだけ自動で書く

**数値はすべて判断ログと約定履歴から決定的に作ります。** 価格の高安は「15分ごとの
観測値であって足の高安ではない」ことを本文に明記し、観測していない値は書きません。

**「所感」は決定的なコードでは埋めません。** いったん `（未記入）` として書き出し、
そのあとの言語化の段で、ナンピノニクス自身の言葉に置き換えます
（`agent.yaml` の `narrate`）。書けなければ `（未記入）` のまま残ります。

言語化は**記録の文章を書くだけ**で、売買の判断には関与しません。
渡すのは本文そのもので、そこにある数値だけを使わせます。

書き出しの規則は2つだけです。

1. 終わった日、または `memory.daily.write_at` を過ぎた当日について書く
2. **すでにあるファイルは上書きしない。** あとから書き足した所感を消さないため

停止していて書けなかった日は、次に動いたときに埋めます。

## 長期：`var/memory/lessons.md`

**建玉が完結したとき（全量決済・時間切れ・強制手仕舞い）にだけ**追記します。
日々の値動きへの感想は書きません。

```markdown
## 2026-08-05 〜 2026-08-19 / btc_jpy / +18,400 JPY (+3.1%)

- 使った段: 3 / 5
- 平均取得単価: 14,300,000 → +0.3% で半分、+0.6% で残りを利確
- 起きたこと: 3段目の約定から4日で戻った
- 学び: アンカーの切り下がりが速い局面では、2段目と3段目の指値が
  近づきすぎる。段の間隔は直前の約定価格ではなく、その時点のアンカーからの
  累積下落率で決めるほうが素直かもしれない。
- 検証したいこと: 段の基準を last_fill から anchor 基準に変えた場合の比較
```

建玉のラウンドは約定履歴から復元します。使った段数・平均取得単価・取得原価・
保有時間・実現損益は決定的に書き、**「学び」は言語化の段で埋めます。**
そこでは「次の判断を変えうること」だけを書かせ、戦略を変えたくなったら
そう書くだけにさせます（自分では変えません）。
同じラウンドを二重に書かないよう、見出しが既にあるものは追記しません。

- 上限 50 件。超えたら古い順に読み直し、重複する学びを1件に統合する
- 「学び」は**次の判断を変えうるもの**だけを書く。感想は書かない
- 戦略そのものを変えたくなったら、ここに書いて**人間の判断を待つ**。
  ナンピノニクスは自分で `agent.yaml` を書き換えません

## 書き込みのルール

1. **数値には出典を添える。** どのコマンドの応答か、いつ取得したかを残す。
   出典のない数値は記憶に書き込まない。
2. **観測しなかったことを書かない。** 取れなかった値は `null`。
3. **損失を言い換えない。** 含み損は含み損、撤退は撤退と書く。
4. **予測を事実として書かない。** 「反発しそう」は判断の根拠にならないので残さない。
5. **秘匿情報を書かない。** API キー、シークレット、プロファイル名は
   ログにも記憶にも一切残さない（`agent.yaml` の `memory.redact`）。

## 忘れかた

- 判断ログ：90日を過ぎた `var/memory/decisions/*.jsonl` を削除する。
  日次サマリに要約が残っているため、失われるのは詳細だけ。
- 日次サマリ・`lessons.md`：削除しない。これがナンピノニクスの実績そのもの。

記憶を消す操作は、**判断ログの期限切れ削除だけ**が自動で行われます。
それ以外の削除は人間が行います。
