# リスク制約 — ナンピノニクス

> 数値パラメータの正は `agent.yaml` です。本書は「なぜその制約なのか」を説明します。

## 原則

ナンピン型戦略の破綻要因はただひとつ、**下落中に資金を使い切ること**です。
本書はその一点を防ぐために存在します。

`personality.md` が「買いたい」と言い、本書が「これ以上は買わせない」と言う。
**両者が矛盾したときは、常に本書が勝ちます。** 例外はありません。

## 資金の制約

| 項目                     | 上限        | 根拠                                     |
| ------------------------ | ----------- | ---------------------------------------- |
| 建玉の取得原価合計       | 初期資金の 60% | 階段5段ぶん（600,000 JPY）で使い切る     |
| 常時維持する現金         | 初期資金の 20% | 何があっても手をつけない緩衝             |
| 1回の発注金額            | 160,000 JPY | 最大の段（5段目）の予算                  |
| 階段の段数               | 5段         | 6段目は存在しない                        |

**60% を超える買いは、条件が揃っていても発注しません。**
残りの 40% は「次の下落のため」ではなく、単に使わない資金です。

## ペース制御

一度の急落で階段を使い切らないための制約です。

- 未約定の買い指値は同時に 2 本まで
- 約定後 30 分は次の段を発注しない
- 1日の約定は 16 回まで

**最速で階段を使い切るのに 2 時間です**（30分 × 4）。日をまたぐ余裕はありません。
そのため、この制約だけでは資金を守れません。守っているのは次の資金の上限と
ドローダウンによる段階制御であり、ペース制御は「同じ値動きで何度も刻まない」
ためのものと位置づけます。

なお、5段すべてを使うにはアンカーから -4.13% の下落が要ります。
実測（2026-07 〜 08 の15分足）では、2時間高値からの -2.0% でも1日 0.26 回でした。

## ドローダウンによる段階制御

総資産（現金＋建玉評価額）の初期資金比で判定します。

| 水準       | 状態          | 動作                                                     |
| ---------- | ------------- | -------------------------------------------------------- |
| 0% 〜 -15% | 通常          | 戦略どおりに動く                                         |
| -15% 以下  | `HIBERNATING` | **新規買いを全面停止。** 板に残った買い指値も取り消す。利確の売り指値は維持する |
| -25% 以下  | `HALTED`      | **全建玉を成行で手仕舞い、以後停止。** 人間の再開が必要   |

`HIBERNATING` では、板に残っている買いの指値も取り消します。**放置すると
約定して建玉が増え、「新規買いを全面停止」が守れないためです。** 利確の売り指値は
そのまま残し、戻ってきたときに利確できるようにします。

`HIBERNATING` から資産が -15% を上回れば通常状態に戻ります。
`HALTED` からは自動では戻りません。

### この設計で許容する最大損失

階段を5段すべて使い切った状態（取得原価 600,000 JPY）から、
建玉が約 -42% になった時点で総資産 -25% に達し、強制手仕舞いとなります。

`strategy.md` の想定例（アンカー 10,000,000 JPY）に当てはめると、
**BTC がアンカーからおよそ半値になった水準**が撤退ラインです。
売買の間隔を短くしても、取得原価の上限が変わらないため、ここは動きません。

つまりナンピノニクスの最悪ケースは、**初期資金の -25% で確定**します。
「下がり続けても買い続ける」ことは、設計として認めていません。

## 市場状態のガード

以下のときは判断そのものを行わず、HOLD として記録します。

- `bitbank status` が正常でない（メンテナンス等）
- `bitbank circuit-break` がサーキットブレイク発動中を返す
- CLI がエラー終了した、または JSON を解釈できなかった

## データ品質のガード

- **5分より古いデータで判断しない。** 取得時刻を検証し、古ければ HOLD。
- **価格は必ず CLI 応答由来。** 記憶や推測から価格を持ち込まない。
- **判断に使ったすべての数値に、出典コマンドと取得時刻を残す**（`memory-policy.md`）。
- 数量は `bitbank pairs` の刻みに丸めてから発注する。

## 発注と取消の競合

`bitbank paper cancel-order` は、取消の前に必ず約定判定を走らせます。
取消のつもりで呼んだ指値が、その直前に約定していることがあります。
利確の売り指値を置き直す場面（買い約定で平均取得単価が動いたとき）で必ず踏みます。

- 取消が `open order not found ... (may have already filled)` を返したときは、
  **失敗として扱いません。約定したものとして扱います。**
- そのときは `bitbank paper trade-history` と `bitbank paper active-orders` で
  実際の状態を読み直し、**その回は新しい注文を出さずに HOLD** します。
- **取消をリトライしません。** 約定済みの注文を追いかけ続けることになります。

古い売り指値を消せなかった回に新しい売り指値を出すと、建玉を超える売り注文が
板に並びます。置き直しは必ず「取消が成功した回」にだけ行います。

## 運用を中断したあとの復帰

`paper tick` が遡れるのは**直近24時間まで**です。24時間を超えて停止すると、
その間に指値へ届いた値動きは評価されません。板に残った指値は、
実際には刺さっていたはずでも約定しないまま残ります。

**24時間を超えて停止した場合、再開時に未約定の指値をすべて取り消します。**
この手順は自動で走ります（判定は `agent.yaml` の `runtime.stale_tick_hours`）。

1. ペーパー口座に触る前に、前回の約定判定からの経過時間を測る
   （`tick` も lazy tick も時刻を進めてしまうため、触る前に測る）
2. 上限を超えていれば、未約定注文をすべて `cancel-order --id=<id>` で取り消す
   （「約定していた」で返るものは前節のとおり約定として扱う）
3. **その回は発注せず HOLD する**
4. 次の回は、実測から状態を組み直して通常どおり判断する

停止していた期間の判断は補完しません。取り消した段は、次の実行で
そのときのアンカーと現在価格から計算し直して出します。

ただし、**撤退の水準（総資産 -25%）に達している場合は手仕舞いが優先されます。**
古い指値の整理より、建玉を落とすほうが先です。

## 実行してはならないこと

```
bitbank trade create-order   # 実資金の発注
bitbank trade cancel-order   # 実資金の取消
bitbank paper reset          # 運用実績の破壊
```

`bitbank trade` は CLI 側でも既定が dry-run で `--execute --confirm=<phrase>` を
要求しますが、ナンピノニクスはそれ以前に**このコマンド群を組み立てません**。

API キー・シークレット・プロファイル名を、ログにも記憶にも書きません。

## 例外時の既定動作

**判断できないときは HOLD。**

例外、タイムアウト、想定外の応答 — すべて「何もしない」に倒します。
発注しないことによる機会損失は許容し、誤発注は許容しません。

## 人間の介入が必要なとき

以下は自動では回復しません。運用者の確認を求めます。

- `HALTED` に到達したとき（強制手仕舞い後）
- 同じ CLI コマンドが 3 回連続で失敗したとき
- `bitbank paper pnl` の建玉と `bitbank paper assets` の残高が一致しないとき
  （`status.yaml` との食い違いは異常の兆候として記録するだけでよい。
  判断には常に実測から導出した状態を使う）
- 想定していない状態遷移が起きたとき

いずれの場合も、原因と最後に成功した観測値を `var/memory/lessons.md` に残します。
